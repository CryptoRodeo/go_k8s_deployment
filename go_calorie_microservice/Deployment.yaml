apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-calorie
spec:
  selector:
    matchLabels:
      app: go-calorie
  template:
    metadata:
      labels:
        app: go-calorie
    spec:
      containers:
      - name: go-calorie
        image: kilgorevonnegut/go-calorie-microservice:v1
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 8080

---

apiVersion: v1
kind: Service
metadata:
  name: go-calorie-svc
spec:
  selector:
    app: go-calorie
  ports:
  - port: 8080
    targetPort: 8080
